{
  "name": "cruisemesh-web",
  "private": true,
  "type": "module",
  "version": "1.0.0",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:redirect": "wrangler deploy --config wrangler.redirect.jsonc",
    "bake-images": "node scripts/bake-images.mjs",
    "check": "node scripts/check.mjs"
  },
  "devDependencies": {
    "@resvg/resvg-js": "^2.6.2",
    "wrangler": "^4.0.0"
  },
  "dependencies": {
    "uqr": "^0.1.3"
  }
}
